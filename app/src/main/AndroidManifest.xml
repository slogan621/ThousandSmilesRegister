<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.thousandsmiles.thousandsmilesregister"
    android:versionCode="1"
    android:versionName="1.0">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="landscape"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LetterSelectActivity"
            android:label="@string/title_activity_letter_select"></activity>
        <activity
            android:name=".NameSelectActivity"
            android:label="@string/title_activity_name_select"></activity>
        <activity
            android:name=".GetPINFromUserActivity"
            android:label="@string/title_activity_get_pinfrom_user"></activity>
        <activity
            android:name=".PrintBadgeActivity"
            android:label="@string/title_activity_print_badge"></activity>
        <activity
            android:name=".MessageActivity"
            android:label="@string/title_activity_message"></activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
    </application>

</manifest>
